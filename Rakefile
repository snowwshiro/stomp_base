# frozen_string_literal: true

require "rake"
require "stomp_base"

namespace :stomp_base do
  desc "Run the Shred Station server"
  task server: :environment do
    puts "Starting Shred Station server..."
    # Add server start logic here
  end

  desc "Run tests for Shred Station"
  task test: :environment do
    puts "Running tests..."
    # Add test running logic here
  end
end

task default: "stomp_base:test"
